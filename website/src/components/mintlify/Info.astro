---
// Mintlify <Info> -> Starlight note aside (no dedicated "info" type in Starlight).
import { Aside } from "@astrojs/starlight/components";
const { title } = Astro.props;
---

<Aside type="note" title={title}>
  <slot />
</Aside>
